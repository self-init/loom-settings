[project]
name = "loom-settings"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dearpygui>=2.2",
    "tomlkit>=0.14.0",
]

[project.optional-dependencies]
dpg = [
    "dearpygui>=2.2",
]
